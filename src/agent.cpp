#include "agent.h"

map<int, Direction> Agent::getActions(GameState *gameState) {
    map<int, Direction> ret;

    return ret;
}

Direction Agent::getAction(int agent_id, GameState *gameState) {

    return Direction::NONE;
}
